# GENERATED by scripts/qa/compile-flow.mjs — DO NOT EDIT.
# Source of truth: qa/journey.json + qa/selectors.json.
# Re-generate:  node scripts/qa/compile-flow.mjs
# Validate:     node scripts/qa/lint-flows.mjs   (fails if this file is stale)
#
# STORE is supplied per-run via --env=STORE=<store>. There is deliberately NO
# flow-level env: block — a baked default out-ranks the CLI flag and writes
# captures to the wrong store dir (hard-won; see runbooks/qa-capture.md).

appId: com.joshapproved.tally
---
- launchApp:
    clearState: true
- extendedWaitUntil:
    visible: { text: "Tally" }
    timeout: 30000
- extendedWaitUntil:
    visible: { text: ".*Nov 2023.*" }
    timeout: 15000
- assertNotVisible: { text: ".*Travel.*" }
- scrollUntilVisible:
    element: { text: ".*Weekly shop.*" }
    direction: DOWN
- tapOn:
    text: ".*Weekly shop.*"
    retryTapIfNoChange: true
- extendedWaitUntil:
    visible: { text: "Travel" }
    timeout: 10000
- scrollUntilVisible:
    element: { text: "Travel" }
    direction: DOWN
- tapOn:
    text: "Travel"
    retryTapIfNoChange: true
- tapOn:
    text: "Save changes"
    retryTapIfNoChange: true
- extendedWaitUntil:
    visible: { text: "Tally" }
    timeout: 10000
- scrollUntilVisible:
    element: { text: ".*Travel.*" }
    direction: DOWN
- assertVisible: { text: ".*Travel.*" }
- waitForAnimationToEnd:
    timeout: 1500
# killApp: real process death on Android; stopApp alias on iOS
# (no true force-kill via the iOS XCTest driver). See runbooks/testing-net.md § chaos net.
- runFlow:
    when:
      platform: Android
    commands:
      - killApp
- runFlow:
    when:
      platform: iOS
    commands:
      - stopApp
- launchApp:
    clearState: false
- extendedWaitUntil:
    visible: { text: "Tally" }
    timeout: 30000
- scrollUntilVisible:
    element: { text: ".*Travel.*" }
    direction: DOWN
- assertVisible: { text: ".*Travel.*" }
